server:
  port: 7777

spring:
  datasource:
    #url: jdbc:mysql://localhost:3306/jpa_test?serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
    url: jdbc:mysql://localhost:3306/spring_html_db?serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
    #username: bitweb
    username: khweb
    #password: dkdlxl123
    password: khdkdlxl@123
    driver-class-name: com.mysql.cj.jdbc.Driver
    dialect: org.hibernate.dialect.MySQL8Dialect
  jpa:
    generate-ddl: true
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update
logging:
  level:
    org:
      hibernate: info